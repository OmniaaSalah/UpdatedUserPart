import { RoundedshadowborderDirective } from './roundedshadowborder.directive';

describe('RoundedshadowborderDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundedshadowborderDirective();
    expect(directive).toBeTruthy();
  });
});
